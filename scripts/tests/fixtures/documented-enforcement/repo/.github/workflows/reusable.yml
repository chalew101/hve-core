name: Fixture Reusable

on:
  workflow_call:

permissions:
  contents: read

jobs:
  scan:
    name: Scan
    runs-on: ubuntu-latest
    steps:
      - run: ./scripts/security/Test-Reusable-Fixture.ps1 -FailOnViolation
